version: 2

models:
  - name: metricflow_time_spine
    description: "Daily time spine required by dbt's semantic layer for time-based calculations."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day